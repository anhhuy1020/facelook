<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.facelock">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:label="@string/app_name">
        <activity android:name="com.example.facelock.ui.HistoriesActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="Histories">
        </activity>
        <activity android:name="com.example.facelock.ui.MainActivity"
            android:label="@string/app_name"/>
        <activity
            android:name="com.example.facelock.ui.LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.example.facelock.ui.SplashAct"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>